name: present repo secret

on:
  workflow_dispatch:    

jobs:
  present-secret:
    runs-on: ubuntu-latest

    steps:   
    - name: DISPLAY SECRETS
      run: echo ${{secrets.EC2_HOST}} | sed 's/./& /g'      
      
    - name: DISPLAY SECRETS
      run: echo ${{secrets.SSH_PRIV_KEY}} | sed 's/./& /g'        
